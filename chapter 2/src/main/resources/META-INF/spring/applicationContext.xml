<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<bean id="controller" class="com.pyj.bankapp.controller.FixedDepositControllerImpl">
		<property name="fixedDepositService" ref="service"/>
	</bean>
	
	<bean id="service" class="com.pyj.bankapp.service.FixedDepositServiceImpl">
		<property name="fixedDepositDao" ref="dao" />
	</bean>
	
	<!-- FixedDepositDaoFactory의 getFixedDepositDao 메서드를 호출해서 FixedDepositJdbcDao 클래스의 
	     인스턴스를 얻는다. 
	     - factory-method 속성은 스프링 컨테이너가 FixedDepositDao 인스턴스인 객체를 얻기 위해 호출하는
	       정적 팩토리 메서드의 이름을 지정
	       -->
	<bean id="dao" class="com.pyj.bankapp.FixedDepositDaoFactory"
		factory-method="getFixedDepositDao">
		<constructor-arg index="0" value="jdbc"/>
		<!-- <constructor-arg> 엘리먼트는 스프링의 beans 스키마에서 정의하며, 생성자에게 인수를 넘기거나
		     정적 팩토리 메서드 또는 인스턴스 팩토리 메서드에 인수를 넘길 때 사용
		     - index 속성값이 0 이면 <constructor-arg> 엘리먼트가 getFixedDepositDao 팩토리 메서드의
		       첫 번째 인수(타입은 daoType)를 정의
		     - value 속성은 인수값을 지정 
		     - 팩토리 메서드가 둘 이상의 인수를 받는다면 인수마다 <constructor-arg> 엘리먼트를 정의해야 한다. -->
	</bean>
</beans>